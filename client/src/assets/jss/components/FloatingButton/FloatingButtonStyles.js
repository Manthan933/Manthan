const FloatingButtonStyles = (theme) => ({
    addicon:{
      margin: 0,
      top: 'auto',
      right: 20,
      bottom: 20,
      left: 'auto',
      position: 'fixed',
    },
  })

export default FloatingButtonStyles